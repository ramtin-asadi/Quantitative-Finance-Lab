# quantitative-finance-lab

A quantitative finance project series 

## Quickstart

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -e .[dev]
```


## Structure

- `book/`: Quarto book (no notebooks yet)
- `src/quantfinlab/`: Python package placeholder